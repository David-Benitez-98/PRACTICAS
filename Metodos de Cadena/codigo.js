/*METODO DE CADENAS
concat() - si una cadena comienza con los caracteres de otra cadena, devuelve true, sino devuelve false.
endswith() - si una cadena termina con los caracteres de otra cadena, devuelve true, sino  duelve false.
includes() - si una cadena puede encontrarse dentro de otra cadena, devuelve true, sino devuelve false.
indexof() - devuelve el indice del primer caracter de la cadena, si no existe, devuelve -1.
lastIndexoF() - devuelve el ultimo indice del primer caracter de la cadena, si no existe, devuelve -1

padStar()[propuesta de Estandar] - Rellenar cadena al principio con los caracteres deseados.
padEnd() [propuesta de ECMA] - Rellenar cadena final con los caracteres deseados.
repeat()- Devuelve la misma cadena pero repetida la cantidad que le indiquemos.

- split () - Divide la cadena como le pidamos
- substring()- Nos retorna un pedazo de la cena que seleccionamos.
-toLowerCase() - Convierte una cadena a minuscula.
-toUpperCase() - Convierte una cadena a mayuscula.
-toString() - metodo devuelve una cadena que representa al objeto especificado.
-trim() - elimina los espacios en blanco al final de una cadena.
-trimStart()-elimina los espacios en blanco al comienzo de una cadena.
-valueOf() - retorna el valor primitivo de un objeto string.
*/

/*Metodo de candena
**CONCAT**
let cadena = "cadena de prueba";
let cadena2 = " cadena 2";

resultado = cadena.concat(cadena2);

document.write(resultado);

-----------------------------------------------------------------------
**STARTSWITH** da verdadero si las expresiones son iguales al comienzo
let cadena = "cadena de prueba";
let cadena2 = "cadena";

resultado = cadena.startsWith(cadena2);

document.write(resultado);

--------------------------------------------------------------
*ENDSWITH** da verdadero si coicide el final
let cadena = "cadena de prueba"; 
let cadena2 = "a";

resultado = cadena.endsWith(cadena2);

document.write(resultado);

--------------------------------------------------------------
**INCLUDES** Busca cadenas de texto
let cadena = "cadena de prueba";
let cadena2 = "cadena";

resultado = cadena.includes(cadena2);

document.write(resultado);
--------------------------------------------------------------
**INDEXOF**
let cadena = "pedro es un tarado imaduro";
let cadena2 = "";

resultado = cadena.indexOf("tarado");

document.write(cadena[6]);
--------------------------------------------------------------------

*/









